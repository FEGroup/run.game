run.Ground = (function () {
  'use strict';

  return run.View.extend({
    defaults: {
      model: null,
      id: -1
    },

    initialize: function (model, type, id) {

    },
    draw: function () {

    }

  });
})();
