run.View = (function () {
  'use strict';

  return run.Class.extend({
    defaults: {

    },

    initialize: function (ctx) {
      this._ctx = ctx;
    },

    draw: function () {

    }
  });
})();
